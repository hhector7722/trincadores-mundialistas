/** Vídeo de entradilla antes del quiz (public/icons/gabri-video.mp4). */
export const QUIZ_INTRO_VIDEO_SRC = "/icons/gabri-video.mp4";

/** Tiempo mínimo de la animación «QUIZ DIARIO» antes del crossfade al vídeo. */
export const QUIZ_INTRO_TITLE_MS = 2200;

/** Crossfade título → vídeo. */
export const QUIZ_INTRO_CROSSFADE_MS = 750;

/** Fade-out final antes de entrar al quiz. */
export const QUIZ_INTRO_OUTRO_MS = 700;

/** Anticipación del outro respecto al final del vídeo (evita corte seco en el último frame). */
export const QUIZ_INTRO_OUTRO_LEAD_S = 0.35;

/** Entrada del contenido del quiz tras la entradilla. */
export const QUIZ_PLAY_ENTER_MS = 700;
