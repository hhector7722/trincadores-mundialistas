"use client";

import { BottomChrome } from "@/components/layout/BottomChrome";

/** Chrome inferior dentro del shell flex (sin portal ni fixed). */
export function TabBarWrapper() {
  return <BottomChrome />;
}
