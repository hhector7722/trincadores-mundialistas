/** Columnas: avatar | nombre | goles local | goles visitante | mvp — ancho según contenido (subgrid). */
export const MATCH_PREDICTIONS_COLUMNS =
  "grid-cols-[max-content_max-content_max-content_max-content_max-content]";

export const MATCH_PREDICTIONS_GRID = `grid ${MATCH_PREDICTIONS_COLUMNS}`;

export const MATCH_PREDICTIONS_SUBGRID_ROW =
  "col-span-5 grid grid-cols-subgrid items-center gap-x-2";
