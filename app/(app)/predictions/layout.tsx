export default function PredictionsLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="tm-porra-layout flex h-full min-h-0 flex-col overflow-hidden">{children}</div>
  );
}
